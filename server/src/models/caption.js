/**
 * Represents a captions associated to one or more memes.
 *
 * @param {number} id The id of the caption.
 * @param {string} text The text of the caption.
 */
function Caption(id, text) {
  this.id = id;
  this.text = text;
}

export default Caption;
